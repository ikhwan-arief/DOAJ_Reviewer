# Support

For usage questions, bug reports, or rule interpretation concerns:

- open a GitHub Issue in this repository,
- include submission type (`raw` or `structured`),
- include `js_mode`,
- attach relevant artifacts (`review-summary.json`, `submission.raw.json`, or `submission.structured.json`).

If the case includes sensitive data, redact personally identifiable information before posting.
